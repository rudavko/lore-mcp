# Eval Suite

The eval runner scripts are not present in the current repository snapshot.

Current status:

- `evals/` contains placeholder directories only.
- `bun run evals/run.ts` does not run because `evals/run.ts` is missing.

If you reintroduce the eval runner, update this page and `README.md` in the same change.
