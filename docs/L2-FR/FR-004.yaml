id: FR-004
title: Query graph triples
phase: 1
statement: >
  When an agent calls query_graph with optional subject, predicate, or object
  filters, the system returns matching triples with pagination so that agents
  can explore the knowledge graph.
traces:
  - Charter 3.1: Deliver the system described in L1-vision.md
  - CC-002: Cross-agent knowledge sharing
verification:
  manual:
    - Create triples, query with subject filter, verify correct triples returned
  machine:
    - E2E: create triples, query_graph with subject filter, assert filtered results
    - E2E: query_graph with cursor, assert pagination works
